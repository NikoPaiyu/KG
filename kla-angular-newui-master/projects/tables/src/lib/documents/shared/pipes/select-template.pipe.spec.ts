import { SelectTemplatePipe } from './select-template.pipe';

describe('SelectTemplatePipe', () => {
  it('create an instance', () => {
    const pipe = new SelectTemplatePipe();
    expect(pipe).toBeTruthy();
  });
});
